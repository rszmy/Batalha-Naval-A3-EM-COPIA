{
    "devCommand": "uvicorn app:app --host 0.0.0.0 --port 8000",
    "builds": [
      {
        "src": "src/URLs.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "src/URLs.py"
      }
    ]
  }